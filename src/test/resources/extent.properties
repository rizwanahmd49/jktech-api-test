# Enable the Spark reporter (the default HTML report format)
extent.reporter.spark.start=true

# Specify the output path for the Spark report
extent.reporter.spark.out=target/SparkReport/SparkReport.html

# (Optional) Configure additional reports like HTML or PDF if needed
extent.reporter.html.start=false
extent.reporter.pdf.start=false

# Add system information (optional)
systeminfo.OS=Windows
systeminfo.User=RizwanAhmad